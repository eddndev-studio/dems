name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  api:
    name: api (rust + postgres)
    # GitHub-hosted: el self-hosted tiene un entorno Rust inconsistente (rustup
    # ausente en algunos runners -> el job falla intermitentemente al instalar
    # el toolchain). ubuntu-latest trae rustup y corre el servicio postgres.
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/api

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: dems
          POSTGRES_PASSWORD: dems
          POSTGRES_DB: dems
        # Host port efímero: el runner self-hosted es compartido y un puerto
        # fijo (5433) colisiona con contenedores huérfanos de runs canceladas
        # ("port is already allocated"). Docker asigna un puerto libre y lo
        # leemos vía job.services.postgres.ports['5432'].
        ports:
          - 5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    env:
      CARGO_TERM_COLOR: always
      RUST_BACKTRACE: 1

    steps:
      - uses: actions/checkout@v4

      - name: DATABASE_URL (puerto efímero del servicio postgres)
        run: echo "DATABASE_URL=postgres://dems:dems@localhost:${{ job.services.postgres.ports['5432'] }}/dems" >> "$GITHUB_ENV"

      - name: Install rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo registry + target
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: apps/api -> target

      - name: cargo fmt
        run: cargo fmt --all -- --check

      - name: cargo clippy
        run: cargo clippy --workspace --all-targets

      - name: cargo test
        run: cargo test --workspace --locked

      - name: openapi.json in sync
        run: |
          tmp=$(mktemp)
          cargo run --quiet --bin openapi-export -- "$tmp"
          if ! diff -q "$tmp" ../../packages/shared/openapi.json > /dev/null; then
            echo "::error::openapi.json out of sync — run 'just openapi-export' and commit."
            diff -u ../../packages/shared/openapi.json "$tmp" | head -60
            exit 1
          fi

  mobile:
    name: mobile (flutter)
    runs-on: [self-hosted, vps2]
    defaults:
      run:
        working-directory: apps/mobile

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.38.5'
          channel: 'stable'
          cache: true

      - name: flutter pub get
        run: flutter pub get

      - name: Run code generation
        run: dart run build_runner build --delete-conflicting-outputs

      - name: flutter analyze
        run: flutter analyze

      - name: flutter test
        run: flutter test
